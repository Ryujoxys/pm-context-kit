interface:
  display_name: "Code Context"
  short_description: "PM-facing code research workflow"
  brand_color: "#2563EB"
  default_prompt: "Use $code-context to research current system behavior before writing product requirements."

dependencies:
  tools:
    - type: "mcp"
      value: "code_context_mcp"
      description: "Read-only code repository context tools"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
